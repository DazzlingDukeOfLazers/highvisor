"""highvisor — a local supervisor for the desktop UI layer.

Observe and control native applications on Windows and macOS over a small,
brain-agnostic RPC. See docs/ for the design. This package is the daemon
skeleton: protocol + engine + server + CLI + a Windows PlatformBackend.
"""
__version__ = "0.0.1"
